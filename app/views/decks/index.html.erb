<%= turbo_frame_tag 'my_decks' do %>
  <div>
    <ul>
      <%= turbo_frame_tag "deck_show" do %>
        <% @decks.each do |deck| %>
          <li><%= link_to deck.name, deck_path(deck) %></li>
        <% end %>
      <% end %>
    </ul>
  </div>
<% end %>
